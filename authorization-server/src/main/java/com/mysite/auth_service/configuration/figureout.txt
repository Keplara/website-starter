[User logs in]
      |
      v
Spring Security creates Authentication -> stored in HttpSession
      |
      v
Spring Session stores HttpSession in Redis (with TTL)
      |
      v
(Optional) SessionTracker adds sessionId to user:sessions:<userId> set, with same TTL